#include "main.h"

/**
 * add - Entry point
 * Description: adds two integers and returns the result
 * @x: parameter 1
 * @y: parameter 2
 * Return: x + y
 */

int add(int x, int y)
{
	return (x + y);
}
